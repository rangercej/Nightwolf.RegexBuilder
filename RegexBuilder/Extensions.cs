using System;
using System.Collections.Generic;
using System.Text;

namespace Nightwolf
{
    public static class Extensions
    {
/*
        public static string ZeroOrOne(this RegexBuilderBase item)
        {
            return string.Format("{0}?", item.ToString());
        }

        public static string ZeroOrMore(this RegexBuilderBase item)
        {
            return string.Format("{0}*", item.ToString());
        }

        public static string OneOrMore(this RegexBuilderBase item)
        {
            return string.Format("{0}+", item.ToString());
        }

        public static string AtMost(this RegexBuilderBase item, int i)
        {
            return string.Format("{0}\\{,{1}\\}", item.ToString(), i);
        }

        public static string AtLeast(this RegexBuilderBase item, int i)
        {
            return string.Format("{0}\\{{1},\\}", item.ToString(), i);
        }

        public static string Between(this RegexBuilderBase item, int min, int max)
        {
            return string.Format("{0}\\{{1},{2}\\}", item.ToString(), min, max);
        }
*/
    }
}
